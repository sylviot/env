#! /bin/bash

echo "-------- DOCKER INFO - $1 ---------"

echo -e "Hostname:\t $(docker inspect -f '{{ .Config.Hostname}}' $1)"
echo -e "IPAddress:\t $(docker inspect -f '{{ .NetworkSettings.IPAddress}}' $1)"

echo -e "\n"
